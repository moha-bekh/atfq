services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    networks:
      - frontend
    env_file:
      - ./.env
    # ports:
    #   - "$PORT:8080"
    restart: unless-stopped
